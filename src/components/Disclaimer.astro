---
import { DISCLAIMER_TEXT } from '../consts';

interface Props {
  text?: string;
  class?: string;
}

const { text = DISCLAIMER_TEXT, class: className = '' } = Astro.props;
---

<div 
  class:list={[
    'bg-background-alt dark:bg-background-alt-dark border-l-4 border-primary-500 dark:border-primary-400 p-md md:p-lg my-lg rounded-md',
    className
  ]}
  role="complementary"
  aria-label="Disclaimer"
>
	<div class="max-w-3xl mx-auto">
		<h3 class="text-lg font-semibold mb-sm text-text dark:text-text-dark">🌟 Sacred Disclaimer</h3>
		<p class="text-neutral-600 dark:text-neutral-400 leading-relaxed m-0" set:html={text} />
	</div>
</div> 