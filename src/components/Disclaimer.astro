---
import { DISCLAIMER_TEXT } from '../consts';

interface Props {
  text?: string;
  class?: string;
  style?: string;
}

const { text = DISCLAIMER_TEXT, class: className = '', style = '' } = Astro.props;
---

<div 
  class:list={[
    'bg-[rgb(var(--color-bg-alt))]/50',
    'border-l-4 border-[rgb(var(--color-primary))]',
    'p-element md:p-block',
    'my-block',
    'rounded-md',
    'shadow-sm',
    'theme-transition',
    className
  ]}
  role="complementary"
  aria-label="Disclaimer"
  style={style}
>
	<div class="flex items-center gap-2 mb-2">
		<span class="text-xl" aria-hidden="true">🌟</span>
		<span class="font-semibold text-[rgb(var(--color-text))]">Sacred Disclaimer</span>
  </div>
	<p class="text-[rgb(var(--color-text-muted))] leading-relaxed m-0" set:html={text} />
</div> 