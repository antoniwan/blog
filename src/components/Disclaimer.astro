---
import { DISCLAIMER_TEXT } from '../consts';

interface Props {
  text?: string;
  class?: string;
  style?: string;
}

const { text = DISCLAIMER_TEXT, class: className = '', style = '' } = Astro.props;
---

<div 
  class:list={[
    'bg-background-alt/50 dark:bg-background-alt-dark/50',
    'border-l-4 border-primary-500 dark:border-primary-400',
    'p-element md:p-block',
    'my-block',
    'rounded-md',
    'shadow-sm',
    'theme-transition',
    className
  ]}
  role="complementary"
  aria-label="Disclaimer"
  style={style}
>
	<div class="flex items-center gap-2 mb-2">
		<span class="text-xl" aria-hidden="true">🌟</span>
		<span class="font-semibold text-text dark:text-text-dark">Sacred Disclaimer</span>
  </div>
	<p class="text-neutral-600 dark:text-neutral-400 leading-relaxed m-0" set:html={text} />
</div> 