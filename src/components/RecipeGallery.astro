---
import Gallery from './media/Gallery.astro';

interface Props {
  images: Array<{
    src: string;
    alt: string;
    caption?: string;
  }>;
}

const { images } = Astro.props;
---

<Gallery
  items={images}
  columns={3}
  gap="lg"
  class="my-8"
/> 